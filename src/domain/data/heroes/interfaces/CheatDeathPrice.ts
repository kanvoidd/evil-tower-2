/**
 * Чем линейка платит, когда талант спасает героя от смерти («Аварийный барьер», «Откупиться»):
 * маг сжигает ману на барьер, наёмник откупается частью золота кошеля, воин и лучник — ничем.
 */
export interface CheatDeathPrice {
  /** Шкала ресурса обнуляется. */
  drainsResource: boolean;
  /** Доля золота из кошеля комнаты, которую герой отдаёт (0 — ничего). */
  goldShare: number;
}
