/**
 * Синергии из пары «шанс / сила»: с шансом `chance` способность срабатывает ещё раз с силой
 * `power`: `boltEcho` — второй разряд «Удара молнии» по той же цели, `basicSplit` — основной удар
 * задевает ещё одного врага.
 */
export type TalentChanceFx = 'boltEcho' | 'basicSplit';
