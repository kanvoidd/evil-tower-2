/**
 * Синергии из пары «шанс / сила»: с шансом `chance` действие срабатывает ещё раз с силой `power`:
 * `basicSplit` — основной удар задевает ещё одного врага, `basicEcho` — после базовой атаки героя
 * (рукой или выстрелом) по той же цели бьёт ещё раз («Поддержка с воздуха»).
 */
export type TalentChanceFx = 'basicSplit' | 'basicEcho';
