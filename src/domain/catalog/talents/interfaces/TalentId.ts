/**
 * Id таланта по смыслу (`thick_skin`): по нему название в словарях (`talent.<id>.name`).
 * Место в дереве — отдельный позиционный id (`TalentPlace.id`), он же ключ сохранения.
 */
export type TalentId = string;
