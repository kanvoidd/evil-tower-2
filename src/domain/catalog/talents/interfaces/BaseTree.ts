import type { TalentTier } from './TalentTier';

/**
 * Дерево «Основа» линейки — общие характеристики героя (здоровье, защита, крит, ресурс): ярусы
 * сверху вниз, в каждом — цепочки путей. Следующий ярус открывается, когда пройдена любая
 * цепочка предыдущего. Лежит в `heroes/<линейка>/talents/base.ts`.
 */
export type BaseTree = readonly TalentTier[];
