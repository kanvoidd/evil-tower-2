import type { ClassDef } from '../../../classes/interfaces/ClassDef';

/** «Маг» — базовый класс. */
export const mage: ClassDef = {
  id: 'mage',
  lineage: 'mage',
  stage: 0,
  parent: null,
  bonuses: {},
};
