import type { Card } from '../../card/Card';

/**
 * Источник карт для движка. Движок сам не решает, какие карты бывают, — он только просит
 * долить колоду перед тем, как вытянуть карту на освободившуюся клетку.
 */
export interface IDeckSupply {
  topUp(deck: Card[]): void;
}
