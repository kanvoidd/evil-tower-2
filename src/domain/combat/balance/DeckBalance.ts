import { Ratio } from '../../shared';

/** Колода комнаты: артефакт и босс в начальной колоде, подсыпка карт, место выхода. */
export const DeckBalance = {
  /** Шанс артефакта в колоде комнаты — у линеек, у которых он есть. */
  artifactChance: Ratio.of(0.7),
  /** Босс ложится в хвост колоды: не раньше этой доли её длины. */
  bossFrom: Ratio.of(0.7),
  /** Колода подсыпается, когда в ней меньше стольких карт… */
  refillBelow: 9,
  /** …до стольких. */
  refillTo: 14,
  /**
   * Состав подсыпки — накопленные границы броска: меньше `enemy` — враг, меньше `gold` —
   * золото, дальше сундук, пустой сундук, зелье исцеления, остальное — зелье восстановления.
   */
  refillMix: { enemy: 0.62, gold: 0.78, chest: 0.84, emptyChest: 0.94, heal: 0.98 },
  /** Открывшийся выход ложится в колоду не глубже стольких карт сверху. */
  exitWithin: 3,
} as const;
