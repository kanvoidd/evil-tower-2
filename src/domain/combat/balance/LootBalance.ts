import { Ratio } from '../../shared';

/** Добыча боя: золото на поле и в сундуках, расходники из сундуков, удача. */
export const LootBalance = {
  /** Шанс, что сундук даст расходник (удача героя добавляет к нему). */
  chestItemChance: Ratio.of(0.35),
  /** Шанс второго расходника сверху. */
  chestBonusItemChance: Ratio.of(0.08),
  /** Каждое очко удачи прибавляет столько к шансу расходника в сундуке… */
  itemChancePerLuck: Ratio.of(0.02),
  /** …и столько к золоту с врагов и из кучек на поле. */
  goldPerLuck: Ratio.of(0.05),
  /** Кучка золота на поле: монет до множителей комнаты и героя (от и до). */
  goldPile: { min: 4, max: 9 },
  /** Золото в сундуке до множителей (от и до). */
  chestGold: { min: 10, max: 24 },
  /**
   * Какой расходник в сундуке — накопленные границы броска: меньше `heal` — зелье исцеления,
   * меньше `regen` — зелье восстановления, дальше — артефакт (он есть не у всех линеек).
   */
  chestConsumables: {
    withArtifact: { heal: 0.4, regen: 0.7 },
    plain: { heal: 0.55 },
  },
} as const;
