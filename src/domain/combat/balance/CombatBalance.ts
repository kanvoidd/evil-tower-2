import { Ratio } from '../../shared';

/** Числа общих правил боя: крит, броня, воскрешение, парирование. */
export const CombatBalance = {
  /** Крит бьёт каждый раз по-разному: множитель урона выпадает случайно между минимумом и максимумом (раз). */
  critMulMin: 1.3,
  critMulMax: 1.9,
  /** Броня снижает входящий удар не больше чем на эту долю. */
  maxDefenseReduction: Ratio.of(0.5),
  /** Воскрешение (за видео и талантом «Возвращение») поднимает героя с этой долей здоровья. */
  reviveHpRatio: Ratio.of(0.6),
  /** Парировав удар, герой отвечает этой долей своего урона. */
  parryCounter: Ratio.of(0.5),
} as const;
