import type { DayKey } from '../../../shared';

/**
 * Календарь игрока: какой сегодня день по его местному времени — награда дня меняется в полночь
 * игрока. Откуда берётся «сегодня», решает среда (реализация — в инфраструктуре); проверки
 * задают день явно.
 */
export interface ICalendar {
  today(): DayKey;
}
