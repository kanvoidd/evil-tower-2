/**
 * Хранилище профиля. Изменения профиля сохраняются сами (с задержкой); `flush` записывает
 * немедленно — в ключевые моменты (конец комнаты, уход из боя), чтобы закрытая вкладка ничего не отняла.
 */
export interface IProfileStorage {
  flush(): void;
}
