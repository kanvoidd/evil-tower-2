/**
 * Источник зёрен для генератора случайностей боя. Каждая комната собирается из нового зерна;
 * откуда берётся энтропия, решает корень композиции, а проверки подставляют свои зёрна.
 */
export interface ISeedSource {
  /** Новое 32-битное зерно без знака. */
  next(): number;
}
