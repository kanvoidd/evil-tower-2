/**
 * Что игрок выбирает после нажатия на способность: цель (`one`), вторую карту (`two`), способность
 * для «Взведённой ловушки» (`skill`); `null` — заряд снят.
 */
export type ArmedPick = 'one' | 'two' | 'skill' | null;
